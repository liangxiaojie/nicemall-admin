query getGoodses($type: Int!) {
  goodses(first: 0, query: {
    type: $type
  }) {
    _id
    imgSrc
    images
    type
    name
    discription
    price
    priceOld
    sales
    store_nums
    mark
  }
}
